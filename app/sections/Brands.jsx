import { DiAppstore } from "react-icons/di";

const techIcons = [];

const Brands = () => {
  return (
    <div className="p-4">
      {/* <h2 className="lg:text-3xl md:text-2xl text-xl text-center font-bold">
        Brands you can get at Anukul Trading Corp
        <span className="text-blue-600">.</span>
      </h2>
      <div>
        <ul className="flex justify-center ite">
          {techIcons.map((item) => (
            <li key={item.icon}>{item.icon}</li>
          ))}
        </ul>
      </div> */}
    </div>
  );
};

export default Brands;
